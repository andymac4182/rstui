//! Google **A2UI** v0.10 — envelope, catalog, data binding, actions,
//! capability negotiation. Placeholder; the implementation lands in a
//! follow-up slice (ADR 0017). Declared now so `lib.rs` stays stable
//! across parallel slices.
