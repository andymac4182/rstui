//! Vercel **json-render** — flat element map, expression resolver,
//! directives, RFC-6902 patch stream, standard components, registry.
//! Placeholder; the implementation lands in a follow-up slice (ADR
//! 0017). Declared now so `lib.rs` stays stable across parallel slices.
