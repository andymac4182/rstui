# Build artifacts: the cdylib target and the platform `.node` are built
# locally (or by the deferred packaging in ADR 0019 phase 5d), never
# committed. Cargo.lock is kept (reproducible proof crate).
/target/
*.node
