# Required substrings in the LAST captured frame of agent-ui.tape (the
# json-render screen). One marker per line; blank lines and lines
# starting with '#' are ignored. `cargo xtask record e2e --check` fails
# if any is missing — i.e. the real crossterm binary failed to boot,
# translate palette input, leave the A2UI editor text-entry screen, or
# project the agent's json-render document.
#
# Stable, tick-counter-independent literals: the header brand, the
# navigated format (header label + panel title), the rendered-output
# panel title, a static token the projected document always renders
# (the json-render sample-1 Heading), and the footer hint.
rstui
json-render
Rendered output
api-gateway
palette
